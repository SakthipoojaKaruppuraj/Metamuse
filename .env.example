# MetaMuse Environment Configuration Template

# Mode Selection: 'real' for Monad Testnet + live Ethereum provenance, or 'mock' for sandbox simulation
NEXT_PUBLIC_APP_MODE=real

# Server-only Secrets (never prefix with NEXT_PUBLIC_)
OPENSEA_API_KEY=
GEMINI_API_KEY=
ETHEREUM_RPC_URL=
